# 🏗️ System Architecture

## 📊 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         Frontend (React)                         │
│                     http://localhost:5173                        │
├─────────────────────────────────────────────────────────────────┤
│  Components  │  Pages  │  Context  │  Services  │  Hooks        │
│  ─────────────────────────────────────────────────────────────  │
│  • UI        │ Login   │ Auth      │ API Client │ useAuth       │
│  • Layout    │ Dashboard│          │ Auth       │ useToast      │
│  • Tasks     │ TaskBoard│          │ Employee   │               │
│  • Dashboard │ Employees│          │ Task       │               │
│              │ MyTasks  │          │ Remark     │               │
│              │ Settings │          │ User       │               │
└──────────────────────┬──────────────────────────────────────────┘
                       │
                       │ HTTP/REST API
                       │ (Axios + JWT)
                       │
┌──────────────────────▼──────────────────────────────────────────┐
│                    Backend (FastAPI)                             │
│                   http://localhost:8000                          │
├─────────────────────────────────────────────────────────────────┤
│  Routers  │  CRUD  │  Models  │  Schemas  │  Middleware         │
│  ──────────────────────────────────────────────────────────────  │
│  • Auth   │ Employee│ Employee │ Pydantic  │ Error Handler      │
│  • Employee│ User   │ User     │ Validation│ Logging            │
│  • Task   │ Task   │ Task     │ Requests  │ CORS               │
│  • User   │ Remark │          │ Responses │                    │
│  • Remark │        │          │           │                    │
└────────┬────────────────────────────────┬─────────────────────┘
         │                                │
         │                                │
    ┌────▼─────┐                    ┌────▼─────┐
    │  MySQL   │                    │ MongoDB  │
    │  :3306   │                    │  :27017  │
    ├──────────┤                    ├──────────┤
    │ employees│                    │ remarks  │
    │ users    │                    │ logs     │
    │ tasks    │                    │          │
    └──────────┘                    └──────────┘
```

## 🔄 Request Flow

### 1. Authentication Flow

```
User Login
    │
    ├─► Frontend (Login.tsx)
    │       │
    │       ├─► AuthContext.login()
    │       │       │
    │       │       └─► authService.login()
    │       │               │
    │       │               └─► POST /api/login
    │       │                       │
    │       ▼                       ▼
    │   Backend (auth_router.py)
    │       │
    │       ├─► Validate credentials
    │       ├─► Query MySQL users table
    │       ├─► Generate JWT token
    │       └─► Return token + user info
    │               │
    ▼               ▼
Store in localStorage
    │
    └─► Redirect to Dashboard
```

### 2. Task Management Flow

```
Create Task (Manager/Admin)
    │
    ├─► Frontend (TaskBoard.tsx)
    │       │
    │       └─► taskService.createTask()
    │               │
    │               └─► POST /api/tasks
    │                       │
    │                       ▼
    │               Backend (task_router.py)
    │                       │
    │                       ├─► Validate JWT
    │                       ├─► Check role permissions
    │                       ├─► Validate request (Pydantic)
    │                       ├─► Create task (SQLAlchemy)
    │                       ├─► Log action (MongoDB)
    │                       └─► Return task
    │                               │
    ▼                               ▼
Update UI + Show Toast
```

### 3. Task Status Update Flow

```
Developer moves task to REVIEW
    │
    ├─► Frontend (KanbanBoard.tsx)
    │       │
    │       ├─► Drag & Drop event
    │       │
    │       └─► taskService.updateTaskStatus()
    │               │
    │               └─► PUT /api/tasks/{id}/status
    │                       │
    │                       ▼
    │               Backend (task_router.py)
    │                       │
    │                       ├─► Validate JWT
    │                       ├─► Check role permissions
    │                       ├─► Validate status transition
    │                       ├─► Update task (MySQL)
    │                       ├─► Log action (MongoDB)
    │                       └─► Return updated task
    │                               │
    ▼                               ▼
Update Kanban board + Notify
```

## 🔐 Security Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     Security Layers                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  1. Frontend Security                                        │
│     ├─► JWT Token Storage (localStorage)                    │
│     ├─► Axios Interceptors (auto-attach token)              │
│     ├─► Protected Routes (AuthContext)                      │
│     └─► Role-based UI rendering                             │
│                                                              │
│  2. Network Security                                         │
│     ├─► CORS Configuration (allowed origins)                │
│     ├─► HTTPS (production)                                  │
│     └─► Request/Response validation                         │
│                                                              │
│  3. Backend Security                                         │
│     ├─► JWT Token Verification (python-jose)                │
│     ├─► Password Hashing (bcrypt)                           │
│     ├─► Role-based Access Control (decorators)              │
│     ├─► Input Validation (Pydantic)                         │
│     └─► SQL Injection Prevention (ORM)                      │
│                                                              │
│  4. Database Security                                        │
│     ├─► Connection credentials (.env)                       │
│     ├─► Prepared statements (SQLAlchemy)                    │
│     └─► Access logging (MongoDB)                            │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 📦 Data Models

### MySQL Schema

```
┌─────────────────────┐
│     employees       │
├─────────────────────┤
│ PK  e_id           │
│     name           │
│     email          │
│     designation    │
│ FK  mgr_id         │
└──────┬──────────────┘
       │
       │ 1:1
       │
┌──────▼──────────────┐
│       users         │
├─────────────────────┤
│ PK  e_id           │
│     password       │
│     role           │
│     status         │
└─────────────────────┘

┌─────────────────────┐
│       tasks         │
├─────────────────────┤
│ PK  t_id           │
│     title          │
│     description    │
│ FK  created_by     │
│ FK  assigned_to    │
│ FK  assigned_by    │
│     assigned_at    │
│ FK  updated_by     │
│     updated_at     │
│     priority       │
│     status         │
│ FK  reviewer       │
│     expected_close │
│     actual_close   │
└─────────────────────┘
```

### MongoDB Collections

```
┌─────────────────────┐
│      remarks        │
├─────────────────────┤
│ _id: ObjectId      │
│ task_id: Number    │
│ user_id: Number    │
│ comment: String    │
│ created_at: Date   │
│ updated_at: Date   │
└─────────────────────┘

┌─────────────────────┐
│       logs          │
├─────────────────────┤
│ _id: ObjectId      │
│ action: String     │
│ endpoint: String   │
│ user_id: Number    │
│ status_code: Number│
│ timestamp: Date    │
│ details: Object    │
└─────────────────────┘
```

## 🎭 Role-Based Access Control

```
┌─────────────────────────────────────────────────────────────┐
│                    Permission Matrix                         │
├──────────────┬──────────┬──────────┬──────────┬─────────────┤
│   Resource   │  Admin   │ Manager  │Developer │   Public    │
├──────────────┼──────────┼──────────┼──────────┼─────────────┤
│ Login        │    ✓     │    ✓     │    ✓     │      ✓      │
├──────────────┼──────────┼──────────┼──────────┼─────────────┤
│ Employees    │          │          │          │             │
│  - View All  │    ✓     │    ✗     │    ✗     │      ✗      │
│  - View Team │    ✓     │    ✓     │    ✗     │      ✗      │
│  - Create    │    ✓     │    ✗     │    ✗     │      ✗      │
│  - Update    │    ✓     │    ✗     │    ✗     │      ✗      │
│  - Delete    │    ✓     │    ✗     │    ✗     │      ✗      │
├──────────────┼──────────┼──────────┼──────────┼─────────────┤
│ Users        │          │          │          │             │
│  - View All  │    ✓     │    ✗     │    ✗     │      ✗      │
│  - Create    │    ✓     │    ✓*    │    ✗     │      ✗      │
│  - Update    │    ✓     │    ✓*    │    ✗     │      ✗      │
│  - Delete    │    ✓     │    ✗     │    ✗     │      ✗      │
├──────────────┼──────────┼──────────┼──────────┼─────────────┤
│ Tasks        │          │          │          │             │
│  - View All  │    ✓     │    ✗     │    ✗     │      ✗      │
│  - View Team │    ✓     │    ✓     │    ✗     │      ✗      │
│  - View Mine │    ✓     │    ✓     │    ✓     │      ✗      │
│  - Create    │    ✓     │    ✓     │    ✗     │      ✗      │
│  - Update    │    ✓     │    ✓     │    ✓**   │      ✗      │
│  - Delete    │    ✓     │    ✗     │    ✗     │      ✗      │
│  - Review    │    ✗     │    ✓     │    ✗     │      ✗      │
├──────────────┼──────────┼──────────┼──────────┼─────────────┤
│ Remarks      │          │          │          │             │
│  - View      │    ✓     │    ✓     │    ✓     │      ✗      │
│  - Create    │    ✓     │    ✓     │    ✓     │      ✗      │
│  - Update    │    ✓***  │    ✓***  │    ✓***  │      ✗      │
│  - Delete    │    ✓***  │    ✓***  │    ✓***  │      ✗      │
└──────────────┴──────────┴──────────┴──────────┴─────────────┘

* Manager can only create/update users for their team
** Developer can only update status of assigned tasks
*** Can only update/delete own remarks
```

## 🔄 Task Workflow State Machine

```
                    ┌──────────────────────────────────┐
                    │         Task Created             │
                    │     (Manager/Admin only)         │
                    └────────────┬─────────────────────┘
                                 │
                                 ▼
                    ┌────────────────────────┐
                    │        TO_DO           │
                    │   (Default Status)     │
                    └────────┬───────────────┘
                             │
                             │ Developer starts work
                             │
                             ▼
                    ┌────────────────────────┐
                    │     IN_PROGRESS        │
                    │  (Developer working)   │
                    └────────┬───────────────┘
                             │
                             │ Developer submits
                             │
                             ▼
                    ┌────────────────────────┐
                    │       REVIEW           │
                    │  (Awaiting approval)   │
                    └────────┬───────────────┘
                             │
                    ┌────────┴────────┐
                    │                 │
         Approved   │                 │  Needs changes
                    │                 │  (Remark required)
                    ▼                 ▼
        ┌───────────────────┐  ┌─────────────┐
        │       DONE        │  │ IN_PROGRESS │
        │ (Task completed)  │  │   (Rework)  │
        └───────────────────┘  └──────┬──────┘
                                      │
                                      └─────► (Back to review cycle)
```

## 📡 API Architecture

### API Endpoint Structure

```
/api
├── /login                      POST   - Authenticate user
├── /employees
│   ├── /                      GET    - List employees (paginated)
│   ├── /                      POST   - Create employee
│   ├── /{e_id}               GET    - Get employee
│   ├── /{e_id}               PUT    - Update employee
│   └── /{e_id}               DELETE - Delete employee
├── /users
│   ├── /                      GET    - List users
│   ├── /                      POST   - Create user
│   ├── /{e_id}               GET    - Get user
│   ├── /{e_id}               PUT    - Update user
│   └── /{e_id}               DELETE - Delete user
├── /tasks
│   ├── /                      GET    - List tasks (paginated, filtered)
│   ├── /                      POST   - Create task
│   ├── /{t_id}               GET    - Get task
│   ├── /{t_id}               PUT    - Update task
│   ├── /{t_id}/status        PUT    - Update task status
│   └── /{t_id}               DELETE - Delete task
└── /remarks
    ├── /{t_id}               GET    - Get task remarks
    ├── /                      POST   - Create remark
    ├── /{remark_id}          PUT    - Update remark
    └── /{remark_id}          DELETE - Delete remark
```

## 🎨 Frontend Component Hierarchy

```
App
├── AuthProvider (Context)
│   └── Router
│       ├── Login (Public)
│       └── DashboardLayout (Protected)
│           ├── Header
│           │   ├── User Menu
│           │   └── Notifications
│           ├── Sidebar
│           │   └── Navigation Links
│           └── Content Area
│               ├── Dashboard
│               │   ├── StatsCards
│               │   └── Charts
│               ├── TaskBoard
│               │   └── KanbanBoard
│               │       ├── Column (TO_DO)
│               │       ├── Column (IN_PROGRESS)
│               │       ├── Column (REVIEW)
│               │       └── Column (DONE)
│               │           └── TaskCard[]
│               ├── MyTasks
│               │   └── TaskList
│               ├── Employees
│               │   └── EmployeeTable
│               └── Settings
│                   └── UserSettings
└── ToastContainer (Notifications)
```

## 🚀 Deployment Architecture (Production)

```
┌─────────────────────────────────────────────────────────────┐
│                      Load Balancer                           │
│                    (nginx/Apache)                            │
└────────────┬──────────────────────────┬─────────────────────┘
             │                          │
    ┌────────▼────────┐        ┌───────▼──────────┐
    │   Frontend      │        │    Backend       │
    │   (Static)      │        │   (FastAPI)      │
    │   nginx/CDN     │        │   Gunicorn       │
    └─────────────────┘        └────────┬─────────┘
                                        │
                          ┌─────────────┴──────────────┐
                          │                            │
                    ┌─────▼─────┐              ┌──────▼──────┐
                    │   MySQL   │              │   MongoDB   │
                    │  (Primary)│              │  (Replica)  │
                    └───────────┘              └─────────────┘
```

---

**📚 For more details, see:**

- `README.md` - Complete documentation
- `SETUP_GUIDE.md` - Setup instructions
- `PROJECT_SUMMARY.md` - Integration summary
